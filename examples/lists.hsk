-- lists usage example --

def do_things, (([] (list {} list), (emptyList {} list)) & ; {}
    (io:puts (number:to_string (hd (hd (tl ($ list)))))),
    (io:puts (hd ($ emptyList))).
)

do_things ([] 'foo', ([] 1, 2, 3), 'bar', baz), ([])

