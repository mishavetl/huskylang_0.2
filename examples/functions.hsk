-- functions usage example --

fn bar, {
    r 'hello'
}
fn foo, {
    t (1 + 1), (2 + 2), (bar)
}
fn baz, {
    1 + 1
}

-- puts (to-string ((type (foo)) # 1))

puts (bar)
puts '\n'
puts (to-string ((foo) # 1))
puts '\n'
puts ((foo) # 2)
puts '\n'
