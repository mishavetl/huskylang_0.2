-- atom usage example --

fn `print sum`, ([] (`number one` {} num), (`number two` {} num), (`number three` {} num)), ;
    io:puts (number:to_string (+ ($ `number one`), ($ `number two`), ($ `number three`))).

`print sum` 1, 2, 3
